export const environment = {
  production: true,
  apiUrl: 'api/'              // taken from environment.ts .. we can remove https://localhost:5001      // kestrel server that will be running api will also be serving our application 
};
